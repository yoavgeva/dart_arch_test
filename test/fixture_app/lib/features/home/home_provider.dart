// ignore_for_file: unused_import
// Depends on auth (cross-feature — violation if home must not use auth)
// Depends on domain (allowed in layered arch)
import 'package:fixture_app/features/auth/auth_service.dart';
import 'package:fixture_app/domain/home_model.dart';

void homeProviderMain() {}
